#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define all(x) (x).begin(), (x).end()

int main()
{
  int d, g;
  cin >> d >> g;
  int p[d];
  int c[d];

  int min = -1;
  for (int bit = 0; bit < (1 << d); ++bit)
    int sum = 0;
    rep(i,d){
      sum = (i *100) * 
    }
  }
}
